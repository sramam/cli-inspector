
import { Interaction, ENTER, UP, DOWN } from '../../..';

export const interactions = <Interaction[]> [{
  prompt: '',
  input: null,
  stdout: /\n  Usage: cli \[options\][\s\S]*Application simple description[\s\S]*enable some baz[\s]*/
}];
